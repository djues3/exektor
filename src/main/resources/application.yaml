ktor:
  development: true
  application:
    modules:
      - com.example.ApplicationKt.module
  deployment:
    port: 8080
exektor:
  docker:
#    host: tcp://localhost:2375
    host: unix:///var/run/docker.sock
  api:
    key: changeit